type Mutation {
    createAccount(
        userName: String!
        nickName: String!
        email: String!
        password: String!
        bio: String
        avatar: String
        zipCode: String
        address: String
        addressDetail: String
        phone: String
    ): Boolean!
}

type Query {
    checkUserName(
        userName: String!
    ):Boolean!
}