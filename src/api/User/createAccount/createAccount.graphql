type Mutation {
    createAccount(
        userName: String!
        email: String!
        password: String
        bio: String
        zipCode: String
        address: String
        addressDetail: String
        phone: String
    ): Boolean!
}

type Query{
    something: String!
}