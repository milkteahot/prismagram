enum ACTIONS {
    EDIT
    DELETE
}

type Mutation {
    editPost(
        id:String! 
        caption: String 
        title: String
        action: ACTIONS!
    ): Post!
}

type Mutation {
    editTag (
        postId: String!
        tagId: [String]
        text: [String!]!
    ): Boolean!
}