


type Query {
    postsOffsetPagination (first: Int, after: Int)
    : [Post!]!
}

type Query {
    seeFeedAll(
        id: String
        first: Int
        after: Int
        skip: Int
    ): [Post!]!
}

type Query {
    seeFeed: [Post!]!
}


