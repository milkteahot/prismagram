


type Query {
    postsOffsetPagination (first: Int, after: Int)
    : [Post!]!
}

type Query {
    seeFeedAll(
        id: String
        first: Int
        skip: Int
    ): [Post!]!
}

type Query {
    seeFeedAnother(
        id: String
        userName: String
        first: Int
        skip: Int
    ): [Post!]!
}

type Query {
    seeFeed: [Post!]!
}


